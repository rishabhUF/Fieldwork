 <%= form_for([@project, @project.floorplans.build], :html => {:multipart => true})  do |f| %>
	<%= f.label :name %><br>
	<%= f.text_field :name %><br>
	<br>
	<%= f.label :image %><br>
	<%= f.file_field :image %><br>
	<br>
	<%= f.submit %>
<% end %>